# WAREHOUSE — AEP absolute vs marginal. voxel 0.1 m.
# Node params from lib_campaign world_aep_params warehouse = N_max 250 / N_term 1000 / step 1.5 /
# g_zero 3.0 (2026-08-18: lowered from 5.0 by user). NBVP node params still IDENTICAL; g_zero is AEP-only.
# Single-variable: only marginal_gain differs (abs=false / marg=true).
WORLD=warehouse
PLANNER=aep
N=10
VOXEL_SIZE=0.1
EARLY_STOP=true
GRACE=70.0
KEEP_FOLDER=multi_series_warehouse_aep
SUMMARY=DECISION_SUMMARY_WAREHOUSE_AEP.txt
CONDITIONS=(
  "WH_AEP_abs|abs"
  "WH_AEP_marg|marg"
)
