devel/
logs/
build/
bin/
lib/
data/

# Ignore Visual Studio Code configuration files
.vscode/
.catkin_tools/
.claude/

# Loose markdown notes/handoffs (READMEs stay tracked)
*.md
!README.md

# Campaign run logs + decision summaries (regenerated each run) and Python caches
variants_logs/
__pycache__/
*.pyc

# Workshop paper draft (kept local, not tracked)
worklog/workshop_paper_draft.txt
current_config.env

# Local network config for closed-loop HIL (real IPs stay local; commit config.sh.template only)
single/motion_planning/tmux/one_drone/closed_loop_hil/config.sh
